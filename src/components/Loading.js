import React from 'react';
import ReactLoading from 'react-loading';

const Loading = () => (
  <ReactLoading
    className="center"
    type="spin"
    color="#444"
    delay={0}
  />
);

export default Loading;
